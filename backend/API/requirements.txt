# ============================================================
# DEPENDENCIAS - Sistema Verificación Documentos Mexicanos
# ============================================================
# numpy / scipy / scikit-image / torch: versiones minimas flexibles para que pip elija
# ruedas precompiladas segun tu Python (p. ej. 3.14 necesita torch>=2.9; fijar == antigua
# fuerza compilar desde fuente y requiere Visual Studio).

# --- API Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # Para upload de imágenes
# Fijar == en 3.14 fuerza pydantic-core sin rueda (compila con Rust).
pydantic>=2.7.1,<3
pydantic-settings>=2.3.0

# --- Visión por Computadora ---
opencv-python-headless>=4.10.0
Pillow>=11.0.0
scikit-image>=0.23.2
numpy>=1.26.4
scipy>=1.13.0

# --- OCR ---
pytesseract==0.3.10
rapidocr-onnxruntime
paddleocr==3.6.0
paddlepaddle==3.3.1

# --- PDF (extracción de páginas, comprobantes, NSS/CURP/actas) ---
pymupdf>=1.24.0
torch>=2.3.0
torchvision>=0.18.0
timm==1.0.3                      # EfficientNet-B3

# --- Lectura de Códigos ---
pyzbar==0.1.9                    # QR y barcodes
pdf417decoder>=1.0.0,<2          # PDF417 (INM); 0.1.3 ya no existe en PyPI
qrcode==7.4.2

# --- Metadatos ---
piexif==1.1.3
hachoir==3.3.0                   # Metadatos generales

# --- Base de datos / cache / JWT ---
# No usados por el código actual (app/ solo verifica con API key; BD por defecto SQLite en config sin conexión activa).
# Si en el futuro se añaden modelos SQLAlchemy, PostgreSQL, Redis o JWT, volver a incluir aquí, p. ej.:
#   sqlalchemy, asyncpg o aiomysql, alembic, redis, hiredis (opcional), python-jose, passlib[bcrypt]
slowapi==0.1.9                   # Rate limiting

# --- Logging ---
loguru==0.7.2

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
pytest-cov==5.0.0
factory-boy==3.3.0

# --- Utilidades ---
python-dotenv==1.0.1
