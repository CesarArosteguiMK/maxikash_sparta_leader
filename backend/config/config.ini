[database]
SERVIDOR = "__SPARTA_HOST_REDACTED__"
PUERTO = 3306
ESQUEMA = __SPARTA_SECRET_REDACTED__
USUARIO = __SPARTA_SECRET_REDACTED__
PASSWORD = "__SPARTA_PASSWORD_REDACTED__"

; Ruta a pdftk para rellenar la solicitud interna (AcroForm). Si está vacía o no existe la clave, se usa el comando "pdftk".
; Ejemplo Windows: pdftk_path = "C:\Program Files (x86)\PDFtk Server\bin\pdftk.exe"
; Ejemplo Linux: pdftk_path = "/usr/bin/pdftk"
[pdf]
; pdftk_path =

; URL base pública de la app (enlaces de documentos, correos). Si está vacía se usa el host de la petición.
; En servidor: poner la URL con la que abren la app, ej. https://tudominio.com o https://tudominio.com/sparta___SPARTA_SECRET_REDACTED__/public
[app]
base_url = ""

[webhook]
GOOGLE_CHAT = "https://chat.googleapis.com/v1/spaces/AAQAwGVaK50/messages?key=__SPARTA_TOKEN_REDACTED__&token=wYYF6GuDNmYD8fFwVtR6TJhRjy03_paqBeX_eUKAOtQ"

[pdf_media]
python_path = "C:\Program Files\Python314\python.exe"

; API de verificación de documentos (Python). Si está configurada, se usa para IDENTIFICACIÓN OFICIAL antes del OCR local.
; En servidor: poner la URL completa donde corre la API, ej. http://localhost:8000 o https://api.tudominio.com
[doc_verificacion]
api_url = "http://127.0.0.1:8000/api/v1/verificar"
api_key = "sparta-__SPARTA_SECRET_REDACTED__-doc-verificacion-key"

[ssh]
; 0 = OpenSSH (localhost Windows), 1 = Plink (PuTTY). Cambiar según el entorno.
ssh_use_plink = 1
; OpenSSH (cuando ssh_use_plink = 1)
ssh_command = C:\Windows\System32\OpenSSH\ssh.exe
; Clave OpenSSH: usar la del proyecto (backend/config/ssh). No usar rutas en Downloads.
ssh_key = C:\xampp\htdocs\sparta___SPARTA_SECRET_REDACTED__\backend\config\ssh\jesusssh4.unknown
; Plink (cuando ssh_use_plink = 1). ssh_hostkey evita "Cannot confirm a host key in batch mode"
ssh_command_plink = C:\xampp\plink.exe
ssh_key_plink = C:\xampp\htdocs\sparta___SPARTA_SECRET_REDACTED__\backend\config\ssh\jesusssh4.ppk
ssh_hostkey = SHA256:FxJtxh17+xcOGTuZlJckk6ARdyVDMJhA3P/wbJFLp/k
; Opcional: ruta a pscp (descarga remoto→local con Plink). Si no se define, se usa mismo directorio que plink.
ssh_pscp = C:\xampp\pscp.exe

; # Descargar PSCP
; Invoke-WebRequest -Uri "https://the.earth.li/~sgtatham/putty/latest/w64/pscp.exe" -OutFile "C:\xampp\pscp.exe"

; Correo para postulaciones (Candidatos).
; DRIVER: sendgrid (recomendado en Windows), smtp o mail.
; SendGrid: MAIL_DRIVER=sendgrid, MAIL_SENDGRID_API_KEY y MAIL_FROM en .env (sin SMTP).
; SMTP: smtp_host, smtp_user, smtp_pass (Gmail: contraseña de aplicación).
; Remitente por defecto:
; PREFERIDO: usar archivo .env en la raíz del proyecto (copiar desde .env.example) con MAIL_SMTP_USER y MAIL_SMTP_PASS; así las credenciales no van en config.ini ni en el repo. .env está en .gitignore.
; Alternativa: descomentar smtp_user y smtp_pass abajo (y no subir config.ini con credenciales al repo).
; Use contraseña de aplicación de Gmail, no la contraseña de la cuenta.
[mail]
mail_from = "lazaro.gonzalez@__SPARTA_SECRET_REDACTED__.com"
mail_from_name = "Capital Humano MaxiKash"
; Días límite para subir documentación (se muestra en el correo al candidato)
dias_limite_documentos = 7
; Contacto para dudas (si está vacío se usa mail_from)
; mail_contacto = "lazaro.gonzalez@__SPARTA_SECRET_REDACTED__.com"
; SMTP: descomentar y completar para enviar correos (en XAMPP/Windows mail() no suele funcionar).
smtp_host = "smtp.gmail.com"
smtp_port = 587
smtp_secure = tls
smtp_user = lrgonzalez033@gmail.com
smtp_pass = yhwuatwcmqjfvgrz

;# puuuebtrzplikbyj   lazaro.gonzalez@__SPARTA_SECRET_REDACTED__.com